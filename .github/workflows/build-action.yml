name: build-action
on: [push]
jobs:
    setup:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v1
            - run: npm install
    build:
        needs: setup
        runs-on: ubuntu-latest
        steps:
            - run: npm run build